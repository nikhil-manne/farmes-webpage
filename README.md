# Farmes Dashboard

A professional React and Vite storefront for Farmes, focused on fresh produce discovery, cart management, order tracking, and customer profile flows.

## Scripts

- `npm run dev` starts the local development server.
- `npm run build` creates a production build.
- `npm run lint` checks the codebase with ESLint.
- `npm run test` runs the Vitest suite.
